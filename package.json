{
  "name": "crypto-lab-syndrome-drain",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "Educational visualizer for the multi-instance security degradation of code-based KEMs (BIKE, HQC, Classic McEliece) — IACR ePrint 2026/517. Computes effective bit-security from published Level-1 parameters, and runs a real toy DOOM search that measures the √M discount at toy scale.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:a11y": "playwright test"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.61.1",
    "typescript": "^5.6.3",
    "vite": "^8.2.1",
    "vitest": "^2.1.8"
  },
  "allowScripts": {
    "esbuild@0.21.5": true
  }
}
